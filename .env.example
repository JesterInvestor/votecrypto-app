# Thirdweb Configuration
# Get your client ID from https://thirdweb.com/dashboard
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=your_thirdweb_client_id_here

# Vote.org Integration
# Since Vote.org doesn't provide a traditional API, we use their web services
# No API key required for basic redirect functionality

# Google Civic Information API (Optional)
# Used for elections and polling location lookup - get API key from Google Cloud Console
# Prefer the server-only `GOOGLE_CIVIC_API_KEY`; `NEXT_PUBLIC_GOOGLE_CIVIC_API_KEY` is accepted as a fallback.
GOOGLE_CIVIC_API_KEY=your_google_civic_api_key_here
NEXT_PUBLIC_GOOGLE_CIVIC_API_KEY=your_google_civic_api_key_here

# Development settings
NEXT_PUBLIC_APP_ENV=development

# Minting configuration
# ERC-1155 contract address on Base to claim token ID 0
NEXT_PUBLIC_MINT_CONTRACT_ADDRESS=0xb26C41D72fe12cDdfA8547C85F4Fdf486FFB9a8b
